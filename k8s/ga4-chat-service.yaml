apiVersion: v1
kind: Service
metadata:
  name: ga4-chat-service
  namespace: experimental
  labels:
    app: ga4-chat
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ga4-chat

